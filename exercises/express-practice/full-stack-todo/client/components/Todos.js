import React from 'react';

const Todos = () => {
    return (
        <div className="todos">
            <h2>Todos Component</h2>
            <p>
                Every component other than <code>App</code> should be within{' '}
                <code>/components</code> like this one.
            </p>
        </div>
    );
};

export default Todos;
