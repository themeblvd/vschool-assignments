/*
 * Include Sass.
 *
 * Include Sass and run it through Webpack build.
 * Will compile separately to /dist/assets/css/main.css.
 */
require( '../scss/main.scss' );

/*
 * Add off-canvase menu toggle.
 */
require( './partials/panel.js' );


/*
 * Add parallax effect to site's background
 * component.
 */
require( './partials/parallax.js' );
